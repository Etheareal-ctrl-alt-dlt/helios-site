OS PAGE — asset step
====================
Drop your two screenshots here (create the folder if needed):

  public/images/os/aurora-screen.png     (Aurora terminal)
  public/images/os/solstice-screen.png   (Solstice home view)

They render INSIDE window frames on the page, so they do NOT need
the transparency/alpha treatment — the frame clips them cleanly with
a thin bezel + label bar. Any aspect ratio works; they scale to the
frame width.

If a screenshot's edges look off against the frame, that's a crop
issue, not a transparency issue — just resend and I'll adjust.
